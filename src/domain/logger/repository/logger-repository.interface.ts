import RepositoryInterface from '@domain/shared/repository.interface';
import Logger from '../entity/logger';

export default interface LoggerRepositoryInterface extends RepositoryInterface<Logger> { }
